<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- GPS position of a walk. FINE = exact, COARSE = from the mobile network. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Required since Android 10 to read the step counter sensor. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- The app still works without a step counter, so the feature is not required. -->
    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SipandStride">

        <!-- Every activity must be declared here. Android refuses to start a class it
             cannot find in the manifest, which is an ActivityNotFoundException crash.
             exported="false" means no other app may launch these screens. -->

        <activity
            android:name=".WalkActivity"
            android:exported="false" />

        <activity
            android:name=".DetailActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- Only MainActivity is exported, because the launcher (a different app) has to
             be able to start it. That is what the intent-filter with LAUNCHER does. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>